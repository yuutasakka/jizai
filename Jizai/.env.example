# Frontend Environment Variables (Safe for client-side)
# These variables are prefixed with VITE_ and will be bundled with the client

# API Configuration
VITE_API_BASE_URL=https://your-api-domain.vercel.app

# Supabase URL (public, safe to expose)
VITE_SUPABASE_URL=https://your-project.supabase.co

# Feature Flags
VITE_SALE_ENABLED=false
VITE_SALE_END_AT=

# Storage Configuration
VITE_STORAGE_WARN_PERCENT=70
VITE_STORAGE_DANGER_PERCENT=90

# Note: Never put API keys, secrets, or sensitive data in VITE_ variables
# All sensitive configuration should be handled server-side via backend API